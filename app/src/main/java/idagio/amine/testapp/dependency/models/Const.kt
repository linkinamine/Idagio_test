package idagio.amine.testapp.dependency.models

class Const {
    companion object {
        const val DRIVER_IN = "driver in the car"
        const val DRIVER_OUT = "driver left the car"
        const val CAR_RUNNING = "Engine running"
        const val CAR_STOPPED = "Engine stopped"
    }
}